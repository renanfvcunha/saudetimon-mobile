import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  content: {
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    width: '80%',
    maxHeight: '80%',
    alignSelf: 'center',
    padding: 16,
    borderRadius: 6,
  },
  option: {
    marginBottom: 6,
  },
  optionTxt: {
    marginBottom: 6,
    fontSize: 16,
  },
  hr: {
    borderBottomColor: 'rgba(56, 53, 53, 0.3)',
    borderBottomWidth: 1,
  },
});

export default styles;
